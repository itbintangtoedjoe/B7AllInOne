export function nameValidator(name) {
  if (!name) return "Name can't be empty";
  return "";
}

export function nikValidator(name) {
  if (!name) return "NIK can't be empty";
  return "";
}

export function lobValidator(name) {
  if (!name) return "LOB can't be empty";
  return "";
}
